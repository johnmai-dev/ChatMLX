import Testing
@testable import Database
import GRDB

@Test func example() async throws {
//    let dbQueue = DatabaseQueue.appDatabase(inMemory: true)
//    
//    try await dbQueue.write { db in
//        var conversation = Conversation(title: "test")
//        try conversation.insert(db)
//        
//        var message = Message(role: "user", content: "66", conversationId: conversation.id!)
//        try message.insert(db)
//    }
//    
//    try await dbQueue.read { db in
//        let conversations = try Conversation.including(all: Conversation.messages).asRequest(of: ConversationWithMessage.self).fetchAll(db)
//        
//        print(conversations)
//    }
}
